'use client';

const ResetButton = () => {
  const handleButton = () => {};

  return (
    <button onClick={handleButton} className="text-xs text-dark-gray">
      새로고침
    </button>
  );
};

export default ResetButton;
